import { FixedFloatPipe } from './fixed-float.pipe';

describe('FixedFloatPipe', () => {
  it('create an instance', () => {
    const pipe = new FixedFloatPipe();
    expect(pipe).toBeTruthy();
  });
});
