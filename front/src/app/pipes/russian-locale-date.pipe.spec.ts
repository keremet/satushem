import { RussianLocaleDatePipe } from './russian-locale-date.pipe';

describe('RussianLocaleDatePipe', () => {
  it('create an instance', () => {
    const pipe = new RussianLocaleDatePipe();
    expect(pipe).toBeTruthy();
  });
});
